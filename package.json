{
  "name": "@mkaliezz/dsh-secret-guard",
  "version": "0.1.0",
  "description": "Fail-closed secret-like payload guard for DeepSeek Harness tool calls.",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": ["lib", "cordis.patch.yml"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test lib/tests/*.test.js"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MkaliezZ/dsh-secret-guard.git"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "devDependencies": {
    "@types/node": "22.10.0",
    "typescript": "5.8.3"
  }
}
